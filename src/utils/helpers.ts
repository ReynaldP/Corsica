// Si vous avez des fonctions utilitaires, exportez-les
export function formatDate(date: string): string {
    // Implémentation
    return date;
  }
  
  // Si le fichier est vide pour l'instant
  export const helpers = {
    // Fonctions à venir
  };